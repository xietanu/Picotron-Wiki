# justify

## Overview

Automatically adjusts element x position based on element and parent horizontal size.

[`Element.x`](/picotron_api/gui/attributes/x/main.md) is then added to the calculated value, functioning as an offset.

## Values

Default value: "left"

Valid values: "left", "center", "right"